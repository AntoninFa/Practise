package swtTestLisko;

import java.util.List;

public interface IEmployeeManagement {

    /**
     * 10: * Generates the initial list of employees. Each employee is represented as a String
     * (Given Name(s), Last Name). New employees (Strings) may be added or removed from
     * this List.
     * 11: * @return the initial list of employees
     * 12:
     */
    List<String> generateInitialEmployeeList();
}

